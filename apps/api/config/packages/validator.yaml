framework:
    validation:
        email_validation_mode: html5
        mapping:
            paths:
                - '%kernel.project_dir%/../../src/Article/Infrastructure/Validate/Article.yaml'
                - '%kernel.project_dir%/../../src/Article/Infrastructure/Validate/ArticleComment.yaml'

        # Enables validator auto-mapping support.
        # For instance, basic validation constraints will be inferred from Doctrine's metadata.
        #auto_mapping:
        #    App\Entity\: []

when@test:
    framework:
        validation:
            not_compromised_password: false
