nelmio_api_doc:
    documentation:
        info:
            title: Owl hexagonal
            description: Owl hexagonal
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []